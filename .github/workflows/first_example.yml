name: My Name Workflow

on: push

jobs:
    first_jobs:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome Message
              run: echo "My First Github Action Job"

            - name: List files
              run: ls
              
            - name: Read files
              run: cat README.md