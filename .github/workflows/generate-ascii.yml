name: Generate ASCII Artwork

on: push

jobs:
    ascii_jobs:
        runs-on: ubuntu-latest
        steps:

        - name: Checkout Repo
          uses: actions/checkout@v5

        - name: Executing Shell Script
          run: ./ascii-script.sh
        
        - name: List Files
          run: ls -ltra